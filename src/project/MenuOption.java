package project;

public enum MenuOption {
	help, login, logout, passwd, search_friends, register, list_friends, invite_friend,
	accept_request, friend_requests, chat, create, chat_room, invite_room,
	load, unsubscribe, msgtimer, topictimer, exit, show_status, sleep, list_rooms
}
